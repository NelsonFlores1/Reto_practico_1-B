name: Automatizacion Resiliente con Latencia Aleatoria

on:
  push:
    branches: [ main ]

jobs:
  run-automation-suite:
    name: Ejecucion de Pruebas y Reporte
    runs-on: ubuntu-latest

    steps:
      - name: 1. Descargar el codigo del repositorio
        uses: actions/checkout@v4

      - name: 2. Configurar entorno Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: 3. Instalar dependencias de pruebas
        run: |
          npm ci
          npx playwright install --with-deps

      - name: 4. Levantar aplicacion con Docker
        run: |
          cd src
          docker compose up --build -d

      - name: 5. Esperar a que localhost este disponible
        run: |
          npx wait-on http://localhost:3000

      - name: 6. Ejecutar prueba (3 veces) en modo headless
        run: |
          npx playwright test tests/registro-async.spec.js --repeat-each=3

      - name: 7. Subir reporte Playwright
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/